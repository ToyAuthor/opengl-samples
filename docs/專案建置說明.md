# 專案建置說明
OpenGL 4.6 是2017年發佈的，現在應該很難找到不支援的電腦了。

## 事前準備

### 必備項目
* 支援 C++20 的編譯器
* CMake 3.15 以上

### 選配項目
SDL2, GLEW, fmt 這三個函式庫也許需要你自己準備，
`opengl-samples/extlibs/lib` 那邊有預先編譯了幾個版本，
如果適合你就簡單了，你不用花工夫準備函式庫，
如果無法使用或不想使用，那就要你自行準備了。

## 編譯程式
用 CMake 建立專案檔，然後編譯，
並沒有什麼特別的手續。
